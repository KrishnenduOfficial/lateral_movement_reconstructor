[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lmr"
version = "0.1.0"
description = "Lateral Movement Reconstructor - Automated DFIR network analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "networkx>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.3.0",
    "mypy>=1.8",
]

[project.scripts]
lmr = "lmr.cli:main"